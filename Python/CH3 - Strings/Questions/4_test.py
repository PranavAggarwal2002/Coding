b = "BZ is a bad  boy"
print(b)
print("replace function is used to remove double space with single space",b.replace("  ", " "))
#strings are immutable that means these functions are not changing the original string but making a new string entirely