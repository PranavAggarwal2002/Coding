s = {} # This is empty dictionary
a = {1,2,3} # now it is a set
b = {1} 
print(type(a),type(b),type(s))

emptyset = set() # s = {} will create empty dictionary
g = {1,2,4,5,76,5,89, 44, 65, 4, 4,5,5,5}
print(g) #in this order will not be maintain so if we need
# for order we use list

#Equal sets if A = B
#Equivalent sets if len(A) = len(B)
# A subset of B
# How many subset (2*n)